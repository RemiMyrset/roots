# Default reviewers for every path. `@OWNER` is a placeholder that the first-run
# checklist (README "First run"; the `first-run` skill) replaces with your GitHub
# user or team. Later rows override earlier ones; add path-specific owners below.
* @OWNER
