/**
 * Sample library entry point. Replace it with real code — it exists so the
 * typecheck, lint, and test gates are honestly green from the first commit, and
 * so the sample app has something to import over `workspace:*`.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`
}
