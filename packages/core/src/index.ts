/**
 * Sample entry point for the @roots/core package. Replace with real code; it
 * exists so the template's typecheck/lint/test gates are honestly green from
 * the first commit.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`
}
